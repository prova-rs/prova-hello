---@meta hello
--- prova-hello — Hello — the registry lifecycle demo plugin (create → release → registered → removed).
---
--- Editor-only type stub for `require("hello")`: it gives consumers completion and signatures
--- and ships nothing at runtime. Keep it in sync with init.lua's public API as the plugin grows.

local hello = {}

--- Greet someone. Replace this with the plugin's real API.
---@param who string
---@return string
function hello.greet(who) end

return hello
